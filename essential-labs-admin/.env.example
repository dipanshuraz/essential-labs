# Copy to .env.production.local for local production builds (optional).
# In Cloudflare Pages → Settings → Environment variables, add the same keys.

# Storefront URL (Panda Bamboo) linked from admin
VITE_STOREFRONT_URL=https://shop.example.com

# API gateway (when wired)
VITE_API_BASE_URL=https://api.example.com
