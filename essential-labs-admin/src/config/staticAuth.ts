/** Static demo credentials — replace with real auth when you wire a backend. */
export const STATIC_LOGIN = {
  email: "admin@essentiallabs.com",
  password: "admin123",
} as const;

export const SESSION_STORAGE_KEY = "essential-labs-admin-session";
