{
  "name": "auth_nextauth",
  "description": "NextAuth.js App Router route. Delegates to src/auth.ts. Used for credentials sign-in and session.",
  "http": {
    "basePath": "/api/auth",
    "methods": ["GET", "POST"]
  },
  "inputSchema": {
    "type": "object",
    "description": "Path-specific; see NextAuth docs. Common: signin with provider credentials.",
    "properties": {
      "callbackUrl": { "type": "string", "description": "Redirect after sign-in" }
    }
  },
  "implementation": {
    "routeFile": "src/app/api/auth/[...nextauth]/route.ts",
    "configFile": "src/auth.ts"
  }
}
