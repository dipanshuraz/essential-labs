# Local default; Railway and Render inject PORT automatically.
PORT=4000
NODE_ENV=development

# Comma-separated browser origins (required for admin/creator SPAs in production).
# Example: https://admin.kiddex.com,https://creators.kiddex.com
CORS_ORIGINS=
